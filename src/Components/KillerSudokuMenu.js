// --- IMPORTS --- //

// LIBRARIES
import React from "react";
// STYLE 
import "./KillerSudokuMenu.css";

// --- COMPONENT --- //
const KillerSudokuMenu = (props) => {
  // let selectedCells = []

  // const getAllSelectedCells = () => {
  //   props.GRID_OBJECT.forEach(cell => {if (cell.isSelected === true) {selectedCells.push(cell)}});
  // }
  // getAllSelectedCells()
  // console.log(selectedCells);

  return (
    <section className="killer_menu">
      bonjour
    </section>
    
  );
  
}

// --- EXPORT --- //
export default KillerSudokuMenu;